<resources
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="primary">@color/primary</item>
        <item name="primaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/primary</item>
    </style>

    <style name="RatingBar" parent="android:style/Widget.Material.RatingBar.Small">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlHighlight">@android:color/white</item>
        <item name="colorControlActivated">@color/accent</item>
        <!-- For lollipop and up -->
        <item name="android:progressBackgroundTint" tools:targetApi="lollipop">@android:color/white</item>
        <item name="android:progressTint" tools:targetApi="lollipop">@color/accent</item>
        <item name="android:secondaryProgressTint" tools:targetApi="lollipop">@color/accent
        </item>
        <item name="android:numStars">10</item>
        <item name="android:isIndicator">true</item>
    </style>

</resources>
